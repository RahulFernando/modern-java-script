'use strict'

const name = 'John Doe'
const greeting = "Hello, "

const message = `${greeting} ${name}!`

const stringMessage = greeting + " " + name + "!";

console.log(message);
console.log(stringMessage);